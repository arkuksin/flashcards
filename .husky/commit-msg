#!/usr/bin/env sh
HUSKY_DIR="$(dirname -- "$0")"
HUSKY_SH="$HUSKY_DIR/_/husky.sh"
if [ -f "$HUSKY_SH" ]; then
  . "$HUSKY_SH"
fi

# Run the script relative to the repo root even if CWD differs
SCRIPT_PATH="$HUSKY_DIR/../scripts/enforce-no-main-commit.js"
node "$SCRIPT_PATH" "$1"
